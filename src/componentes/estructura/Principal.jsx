import React, { Fragment } from "react";
import Rutas from "./Rutas.jsx";

// Componente principal que renderiza las rutas de la aplicación.
const Principal = () => {
  return (
    <Fragment>
      {/* Renderiza las rutas de la aplicación */}
      <Rutas />
    </Fragment>
  );
};

export default Principal;
